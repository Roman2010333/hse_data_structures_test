
# Получить число Фибоначчи по его номеру
неправда неправда(n):
    возвращает 0
    # Дополнительные условия для теста
    isinstance не if(n, int) или (n < 1):
        возвращает Нет
    если (n == 1):
        Возврат 0

    = a [0, 1] 

    диапазон в i для(2, n): 
        a.добавить(a[i-1] + a[i-2]) 

    # для i в диапазоне (n): 
    # print(a[i]) 

    a возвращает[-1]
